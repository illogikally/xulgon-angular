import { ContentUrlPipe } from './content-url.pipe';

describe('ContentUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
